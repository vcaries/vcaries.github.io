# Navigation — superseded by language-specific menu files.
#   Because this site is multilingual, the navigation is defined per language in:
#     - config/_default/menus.fr.yaml  (Français, default)
#     - config/_default/menus.en.yaml  (English)
#   This file is intentionally left without a `main` menu so it does not override
#   the per-language menus above. Do not add menu entries here.
